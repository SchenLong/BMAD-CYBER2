# Phase Gate Orchestrator Workflow
name: phase-gate
description: "Validate phase gate requirements before project phase transitions"
author: "BMad"

# Critical variables from config
config_source: "{project-root}/_bmad/core/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Workflow components
installed_path: "{project-root}/_bmad/core/workflows/team-orchestration/phase-gate"
instructions: "{installed_path}/instructions.md"

# Phase gate definitions
gate_definitions: "{installed_path}/phase-gates.yaml"

# Project registry for context
project_registry: "{output_folder}/project-registry.yaml"

standalone: true
