# Select Party Mode Preset Workflow
name: select-preset
description: "Select a pre-configured cross-module agent group for Party Mode"
author: "BMad"

# Critical variables from config
config_source: "{project-root}/src/core/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Workflow components
installed_path: "{project-root}/src/core/workflows/party-mode/select-preset"
instructions: "{installed_path}/instructions.md"

# Presets data
presets_file: "{project-root}/src/core/workflows/party-mode/presets/cross-module-groups.yaml"
party_mode_workflow: "{project-root}/src/core/workflows/party-mode/workflow.md"

standalone: true
