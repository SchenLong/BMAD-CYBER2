# Cross-Module Consultation Workflow
name: cross-module
description: "Identify and invoke cross-module expertise for current context"
author: "BMad"

# Critical variables from config
config_source: "{project-root}/src/core/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Workflow components
installed_path: "{project-root}/src/core/workflows/project-manager/cross-module"
instructions: "{installed_path}/instructions.md"

# Expertise map for cross-module recommendations
expertise_map: "{project-root}/src/core/workflows/project-manager/data/module-expertise-map.yaml"

# Agent manifest for agent lookup
agent_manifest: "{project-root}/_bmad/_config/agent-manifest.csv"

standalone: true
